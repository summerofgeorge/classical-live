{
  "name": "classical-live",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "refresh": "node scripts/refresh.mjs",
    "test": "node --test",
    "start": "node scripts/serve.mjs"
  },
  "dependencies": { "cheerio": "1.1.2" }
}
